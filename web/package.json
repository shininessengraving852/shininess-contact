{
  "name": "web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "prebuild": "npm run clean",
    "clean": "npx --package typescript tsc --build --clean",
    "build": "npx --package typescript tsc --build",
    "postclean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
